services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: cid
      POSTGRES_PASSWORD: cid
      POSTGRES_DB: cid
    ports:
      - "5433:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cid -d cid"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres-data:
